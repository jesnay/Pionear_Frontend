export default class TaskElement {
  constructor() {}

  textBoxInactive() {
    //Leeres Textfeld
  }
  textBoxActive() {
    //Bei anklick nimmt Textbox Userinput an
  }
  openCamera() {
    //öffnet die Kamera -> In unserem Fall fügt automatisch Bild ein
  }
  surveyInactive() {
    //Umfrage ohne Antwort
  }
  surveyActive() {
    //Umfrage -> User hat Antwort angeklickt
  }
}
